### F-136 [HIGH] WatchtowerService threshold hardcoded 3 ≠ GUARDIAN_THRESHOLD=2

| Field | Value |
|-------|-------|
| **ID** | F-136 |
| **Severity** | HIGH |
| **Category** | B2 (config) |
| **Bug pattern** | `watchtower-threshold-hardcoded` |
| **Fingerprint** | `sha256("B2:WatchtowerService.kt:199:threshold-hardcoded-3")` → `f136...` |
| **Status** | NEW |
| **Found** | External Audit Wave 15 (2026-07-01) |

**Description:**
`WatchtowerService.kt:199`: `if (approvals >= BigInteger.valueOf(3))` при `GUARDIAN_THRESHOLD=2` (SocialRecoveryModule.sol:43). Push-уведомление "threshold reached" никогда не сработает для 2 guardian'ов.

**Impact:** HIGH — guardian'ы не получают push при достижении порога.

**Required fix:**
```kotlin
val threshold = config.getInt("watchtower.approvalThreshold", 2)
// вместо BigInteger.valueOf(3)
```
Либо читать из контракта (on-chain GUARDIAN_THRESHOLD).

**Cross-ref:** SocialRecoveryModule.sol:43 — GUARDIAN_THRESHOLD = 2
