### F-137 [HIGH] SwapService — minAmountOut parsed but not in calldata (100% MEV)

| Field | Value |
|-------|-------|
| **ID** | F-137 |
| **Severity** | HIGH |
| **Category** | A3 (swap) |
| **Bug pattern** | `swap-no-slippage-calldata` |
| **Fingerprint** | `sha256("A3:SwapService.kt:106:slippage-not-in-calldata")` → `f137...` |
| **Status** | NEW |
| **Found** | External Audit Wave 15 (2026-07-01) |

**Description:**
`SwapService.kt`:
- `SwapRequest.minAmountOut` существует (l.51) и парсится (l.98) — но **не включён в calldata** (l.106-111)
- `Uint256(minAmountOut)` отсутствует в списке аргументов функции
- 100% slippage exposure при исполнении

**Impact:** HIGH — MEV bots могут сэндвич-атаковать каждый swap. Пользователь получает любое количество токенов.

**Required fix:**
```kotlin
// SwapService.kt:106 — добавить Uint256(minAmountOut) в inputs
listOf(
    Uint256(amountIn),
    Uint256(minAmountOut),   // ← добавить
    DynamicArray(Address::class.java, path.map { Address(it) }),
    Address(request.recipient),
    Uint256(deadline),
)
```
