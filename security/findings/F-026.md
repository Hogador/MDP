### F-026 [LOW] Hardcoded PostgreSQL password

| Field | Value |
|-------|-------|
| **ID** | F-026 |
| **Severity** | LOW |
| **Category** | D4 (default-cred) |
| **Bug pattern** | `hardcoded-password` |
| **Fingerprint** | `sha256("D4:docker-compose.yml:POSTGRES_PASSWORD:hardcoded-password")` |
| **Status** | CLAIMED_FIXED |
| **Found** | Wave 4 (2026-06-24) |
| **Fixed** | 2026-07-07 — all `:-mdaopay` replaced with `:?required` in 3 files |

**Description:**
`POSTGRES_PASSWORD: mdaopay` hardcoded in docker-compose.yml.

**Lifecycle:**
- Wave 4: Found (severity MEDIUM)
- Wave 5: Claimed fixed (premature — only root postgres service fixed)
- Wave 6-7: CONFLICT — `db-backup` service and `backend/docker-compose.yml` still had `:-mdaopay` default
- Wave 16 (2026-07-07): All 3 occurrences now `:?POSTGRES_PASSWORD is required`

**Fix applied (2026-07-07):**
- `docker-compose.yml:29` (db-backup): `:-mdaopay` → `:?POSTGRES_PASSWORD is required`
- `backend/docker-compose.yml:8`: `:-mdaopay` → `:?POSTGRES_PASSWORD is required`
- `docker-compose.yml:9` (postgres) was already correct: `:?POSTGRES_PASSWORD is required`

**Verification recipe:**
```bash
grep "POSTGRES_PASSWORD" docker-compose.yml backend/docker-compose.yml
# PASS: ${POSTGRES_PASSWORD:?required} (fail-fast) — no :-default or hardcoded values
# FAIL: mdaopay (hardcoded)
```

---

